openapi: 3.2.0
info:
  title: Dataverse Ingest API
  description: Open source research data repository software.
  version: 6.10.1
tags:
- name: Ingest
paths:
  /ingest/test/file:
    get:
      operationId: TestIngest_datafile
      parameters:
      - name: fileName
        in: query
        schema:
          type: string
      - name: fileType
        in: query
        schema:
          type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
      tags:
      - Ingest
      summary: Test ingest datafile
      x-summary-source: derived
